let personName = "Aditya";

personName = "Kumar";

console.log(personName); // Output: Kumar

const school = "St. karen's secondary school";
school = "New School"; // This will throw an error

console.log(school);

// so if const keyword is used then the value cannot be changed later.